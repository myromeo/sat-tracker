version: '3.8'

services:
  sat-engine:
    image: ghcr.io/myromeo/sat-tracker:latest
    container_name: sat-global-engine
    restart: unless-stopped
    volumes:
      - tar1090-data:/data
      
volumes:
  tar1090-data:
